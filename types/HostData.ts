type HostData = {
  id: number;
  user: string;
  displayName: string;
  discordName: string;
  handle: string;
  avatarUrl: string;
  for: string[];
  against: string[];
};

export default HostData;
